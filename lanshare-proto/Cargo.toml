[package]
name = "lanshare-proto"
version = "0.1.0"
edition = "2024"

[dependencies]
sha2 = { version = "0.10.9", default-features = false, features = ["std"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
